<div id="slider">
    <div class="row">
        <div class="col-lg-4" style="width: 300px;">
            <div class="slider-text">
                <p class="lead">Watatatow!</p>
                Lorem ipsum dolor sit amet
            </div>
        </div>
        <div class="col-lg-8" style="width: 670px;">
            <ul class="slider list-unstyled">
                {% for image in page.images %}
                    <li>{% if image.href %}<a href="{{ image.href }}">{% endif %}<img class="hide" onload="nl(this);" src="{{ image.pathname('filename') }}" alt="0">{% if image.href %}</a>{% endif %}</li>
                {% endfor %}
            </ul>
            <ul class="carousel list-unstyled">
                {% for image in page.images %}
                    <li></li>
                {% endfor %}
            </ul>
        </div>
    </div>
</div>
